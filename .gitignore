# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.ruff_cache/
*.egg
# Virtual environments
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo

# Reports
reports/

# Coverage
.coverage
htmlcov/
coverage.xml

# DrBrain data (all generated at runtime)
data/

# Local config (contains secrets)
config.local.yaml

# MinerU temp output
mineru_*/

# Superpowers tooling docs (plans/skills are local, specs are project docs)
docs/superpowers/plans/
docs/superpowers/skills/

# Learning notes (open source project studies)
docs/learn/
# Added by code-review-graph
.code-review-graph/
.mcp.json
.claude/
.coverage.*

# Local-only tooling directories (not for public repo)
.trellis/
.qoder/
docs/superpowers/specs/

# All research artifacts (scripts/reports/data/plugins) live here and are local-only
research/

# Local agent tooling (per-user / generated — never commit)
.qwen/
.agents/
.codegraph/

# Runtime caches (API cache responses)
cache/

# Research-loop run workspaces (generated per-topic outputs: autoresearch*)
workspace/

# Test-run artifacts (test API keys in config.yaml + local DBs — never commit)
test-run/

