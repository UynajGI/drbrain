# 本方案已被替换

2026-09-15。当前唯一的设计入口是 [统一 Tree RAG：结构约束的软聚类与证据导航](unified-tree-rag-design.md)。

旧版“四路分别检索、两套层次相接、每篇保留 content.md”的方案已撤回。

当前设计是 BM25、vector、tree 三路；tree 采用统一节点和成员关系、结构条件化软分组、共享 IndexModel 和单个导航过程。规范化正文与树在主 SQLite 中统一管理；不自动生成 Markdown/JSON/SDK 文献副本。

状态：设计与源码审阅已完成，产品实现与验收尚未开始。历史已入库材料不在本次文档修订中迁移或删除。
